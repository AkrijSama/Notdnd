# NOTDND Environment Variables
# These are optional fallbacks when users don't provide their own keys (BYOK)

# Port configuration
PORT=3000
NODE_ENV=development

# AI Provider API Keys (Optional - for self-hosted deployments)
# Users can override these by configuring their own keys in the Settings UI
# OPENAI_API_KEY=sk-...
# GROK_API_KEY=xai-...
# GEMINI_API_KEY=...
# ANTHROPIC_API_KEY=sk-ant-...
# OPENROUTER_API_KEY=sk-or-...
